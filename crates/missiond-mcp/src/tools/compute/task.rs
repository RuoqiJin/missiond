// 🤖 GENERATED BY FORGE - DO NOT EDIT
// Source: .missiond/intent-tools.lisp (module task)

use crate::ToolDefinition;
use serde_json::json;

pub fn definitions() -> Vec<ToolDefinition> {
    vec![
        // ===== mission_task_submit =====
        ToolDefinition::new(
            "mission_task_submit",
            "提交任务给专家 Agent。async: 异步提交; sync: 同步等待结果",
            json!({
                "type": "object",
                "required": ["role"],
                "properties": {
                    "action": {"type": "string", "enum": ["async", "sync"], "default": "async"},
                    "role": {"type": "string", "description": "专家角色(如 secret/deploy/memory)"},
                    "prompt": {"type": "string", "description": "[async] 任务提示词"},
                    "question": {"type": "string", "description": "[sync] 问题"},
                    "slotId": {"type": "string", "description": "指定目标工位 ID(跳过 role 匹配)"},
                    "timeoutMs": {"type": "number", "description": "[sync] 超时毫秒数", "default": 120000}
                }
            }),
        ),
        // ===== mission_task_query =====
        ToolDefinition::new(
            "mission_task_query",
            "任务状态查询。status/list/ack/track",
            json!({
                "type": "object",
                "required": ["action"],
                "properties": {
                    "action": {"type": "string", "enum": ["status", "list", "ack", "track"]},
                    "taskId": {"type": "string", "description": "[status/track] 任务 ID"},
                    "status": {"type": "string", "description": "[list] queued/running/done/failed"},
                    "limit": {"type": "integer", "description": "[list] 最大返回数", "default": 20},
                    "since": {"type": "integer", "description": "[ack] finished_at > since(epoch ms)"}
                }
            }),
        ),
        // ===== mission_task_cancel =====
        ToolDefinition::new(
            "mission_task_cancel",
            "取消任务",
            json!({
                "type": "object",
                "required": ["taskId"],
                "properties": {
                    "taskId": {"type": "string"}
                }
            }),
        ),
    ]
}
