(intent
  :id "20260524-jarvis-db-poll-failfast"
  :goal "Bound Jarvis BoardTask/notes DB polling so public SSE follow streams fail fast with typed diagnostics instead of hanging past mobile/proxy budgets."
  :user-request "Continue iOS Jarvis intent/plan gate validation; fix Agy follow path that timed out while task completed."
  :constraints ["SSOT first" "no fallback" "GitHub sync to Mac mini" "no rsync"])
