(intent
  :id "20260524-jarvis-dispatch-immediate-pending"
  :goal "Make Jarvis plan-confirmed dispatch return a BoardTask handle immediately instead of binding mobile/public SSE to long worker supervision."
  :user-request "Continue iOS Jarvis validation; Codex worker dispatch timed out client-side while the BoardTask later completed."
  :constraints ["SSOT first" "no fallback" "GitHub sync to Mac mini" "no rsync"])
