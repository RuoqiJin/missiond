#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

node scripts/missiond-work-order.mjs verify --staged ${MISSIOND_WORK_ORDER:+--id "$MISSIOND_WORK_ORDER"}
