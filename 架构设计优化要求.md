## 结论

有改善，而且改善方向是对的：新版确实从“PTY/Board 文本驱动状态”往“typed artifact + capability + lease + runtime_metadata + job state”推进了。

但我会很尖锐地说：

> 这次改进像是 LLM 把我的上一轮 critique 翻译成了更多表、更多字段、更多 checker、更多 runtime_metadata；方向变聪明了，但核心执行内核还没有真正变小、变硬、变不可绕过。

最危险的不是没改，而是出现了新的“看起来聪明”的不一致：**schema 和 Rust 写入逻辑不匹配，capability 还不是主体绑定的 capability，work_leases 还不是唯一真实租约权威，control_plane_kernel 目前更像 facade，而不是 kernel。**

我会给新版这个评价：

| 维度                     |     上版 |           新版 |
| ---------------------- | -----: | -----------: |
| 控制面设计方向                | 5 / 10 |   **7 / 10** |
| 不让 PTY/prose 直接关闭任务    | 4 / 10 | **7.5 / 10** |
| capability / lease 硬约束 | 4 / 10 |   **6 / 10** |
| schema-code 一致性        | 5 / 10 |   **4 / 10** |
| 第一性原理瘦身程度              | 3 / 10 |   **3 / 10** |

新版更聪明了，但也更庞大了，而且新增的硬控制面目前有几处会在真实运行时卡住。

---

## 我确认到的改善

这次不是表面改名，确实有实质变化。

你新增了：

```text
crates/missiond-daemon/src/engine/control_plane_kernel.rs
crates/missiond-core/migrations/20260527000000_control_plane_kernel.sql
crates/missiond-core/migrations/20260527001000_runtime_verifier_router_outcomes.sql
scripts/check-v3-control-plane-kernel-isomorphism.mjs
scripts/backfill-board-runtime-metadata.mjs
```

新版比旧版：

```text
+26 个文件
+71 个修改文件
0 个删除文件
```

Rust 从约 **27.7 万行**涨到约 **28.1 万行**。全仓库按源码/文档/脚本算，已经约 **53.6 万行**：

| 类型        | 文件数 |      行数 |
| --------- | --: | ------: |
| Rust      | 758 | 281,554 |
| MJS/JS 脚本 | 177 |  99,076 |
| Lisp      | 768 |  76,308 |
| Markdown  | 341 |  39,928 |
| TS/TSX    | 133 |  25,080 |
| OCaml     |  19 |   5,513 |
| SQL       |  43 |   2,018 |

几个关键改善：

第一，`worker_settle(done)` 现在要求 `artifact_hash`，不再允许纯文本 summary 直接完成任务。`task_result_put` 也会验证 artifact、capability 和 write scope。这是大方向上最重要的进步。

第二，`BoardTask.description` 不再是主要控制字段来源。你把 `write_scope`、`must_not_touch`、`sandbox_profile`、`capability_grant_ids` 这些放进了 `runtime_metadata`。这比从 markdown 里 parse 控制字段聪明很多。

第三，claim 加了数据库级 backstop。`shared_claims` 新增了 active scope unique index，并且代码里使用了 `pg_advisory_xact_lock`。这个明显修正了我之前指出的并发 claim race。

第四，Board 层更新 done 状态时会要求 completion evidence。`update_board_task` 对 `status=done` 调了 `require_task_completion_evidence`。这说明你开始把“关闭任务”从 UI 操作变成受证据约束的状态迁移。

第五，新增了 `jobs`、`job_attempts`、`capability_grants`、`work_leases`、`board_task_views`、`model_route_outcomes`，说明你已经在尝试把 Board、PTY、provider prose 降级为 projection/observation。

这些都是对的。

---

# 但是：新版最不聪明的地方

## 1. 新增 schema 和 Rust 代码已经不一致

这是最严重的问题，因为它不是理念问题，是运行时会炸的问题。

### `capability_grants.operation` 不允许 `spawn`，但 Rust 会写 `spawn`

migration 里：

```sql
operation TEXT NOT NULL CHECK (
  operation IN ('read', 'write', 'claim', 'settle', 'delegate', 'deploy', 'network')
)
```

但是 `grant_task_capabilities` 里会插入：

```rust
operation: "spawn"
```

`mission_compute_slot(create)` 又会检查：

```rust
require_capability(task_id, "spawn", "task", task_id)
```

结果是：**正常 delegate 时插入 spawn grant 会违反 CHECK；即使绕过插入，compute_slot 也永远查不到合法 spawn capability。**

这说明新版的“capability hardening”有一部分是概念先行，schema 没闭合。

应该立刻修：

```sql
operation IN ('read', 'write', 'claim', 'settle', 'delegate', 'deploy', 'network', 'spawn')
```

或者不要用 `spawn` 这个 operation。

---

## 2. `subject_kind` 不允许 `system`，但 kernel 会写 `system`

migration 里：

```sql
subject_kind IN ('worker', 'conversation', 'task')
```

但 `ControlPlaneKernel::complete_system_task` 调：

```rust
grant_task_capabilities(..., "system", producer_id, ...)
```

这也会违反 CHECK。

要么 schema 加：

```text
system
operator
daemon
```

要么 `complete_system_task` 不该走普通 capability grant，而应该走专门的 system authority path，并且 audit 里标明是 kernel/system mutation。

---

## 3. `model_route_outcomes` 表和 Rust 插入列完全对不上

migration 创建的是：

```text
prompt_tokens
completion_tokens
cost_usd
outcome JSONB
status
request_id
route
decision
job_state
```

Rust 插入的是：

```text
input_tokens
output_tokens
cost_micros
job_id
outcome
metadata
```

Rust 查询还做：

```sql
AVG(CASE WHEN outcome IN ('completed','success','accepted') THEN 1.0 ELSE 0.0 END)
AVG(NULLIF(cost_micros, 0))
```

但 migration 里 `outcome` 是 JSONB，不是 text；也没有 `cost_micros`、`input_tokens`、`output_tokens`、`job_id`、`metadata` 这些列。

所以这个 router learning loop 现在大概率直接 runtime SQL error。

这件事很典型：**你新增了“更智能的在线路由学习表”，但没有让 schema 和执行代码形成一个真实闭环。**

这个比没有表更危险，因为它给系统一种“我已经有学习机制”的错觉。

---

## 4. `work_leases` 现在是幽灵权威

你新增了 `work_leases`，这方向很好。但当前它不是可靠 authority。

`claim()` 做的是：

```text
INSERT INTO shared_claims
然后 INSERT INTO work_leases ... ON CONFLICT DO NOTHING
最后返回 shared_claims 成功
```

问题是：

如果 `work_leases` 冲突了，`ON CONFLICT DO NOTHING` 会静默忽略，但函数仍然返回 claim 成功。
release 时只更新 `shared_claims`，没有同步释放 `work_leases`。
heartbeat 时也只更新 `shared_claims`，没有同步 `work_leases`。
`release_claims_for_task()` 也只动 `shared_claims`。

所以现在存在两个坏情况：

```text
shared_claims 说 active，work_leases 没有
shared_claims 已 release，work_leases 仍 active
```

这就变成了“双权威漂移”。

如果 `work_leases` 是真正 lease authority，那 claim/release/heartbeat 都必须只以它为准。
如果 `shared_claims` 才是 authority，那 `work_leases` 不应该参与控制，只能是 projection。
现在这个中间态不聪明。

建议二选一：

```text
方案 A：删掉 work_leases，保留 shared_claims 为唯一租约表。
方案 B：迁移到 work_leases，shared_claims 变兼容 view/projection。
```

不要两个都写。

---

## 5. capability 还不是 capability，只是 task-level policy flag

这是新版最大设计遗憾。

`require_capability` 当前查询条件大致是：

```sql
WHERE task_id = $1
  AND operation = $2
  AND scope_kind = $3
  AND scope_key = $4
  AND status = 'active'
```

它没有检查调用者是谁。

也就是说，只要某个 task 有 active write/settle capability，任何能拿到这个 `task_id` 的调用路径，都可以满足：

```text
write task result
settle task
spawn worker
```

这不是 capability token。
这是“task 有某个权限”。

真正的 capability 应该绑定：

```text
subject_kind
subject_id
grant_id
operation
scope
expiry
issuer
```

每次敏感操作都应该带：

```json
{
  "grant_id": "...",
  "subject_kind": "worker",
  "subject_id": "slot-abc",
  "task_id": "...",
  "operation": "settle"
}
```

daemon 验证：

```text
这个 grant 是否存在
这个 grant 是否属于这个 subject
这个 subject 是否就是当前调用者
这个 operation/scope 是否匹配
这个 grant 是否过期/撤销/已消费
```

现在 `capability_grants` 有这些字段，但 enforcement 没用上。
这就是“字段聪明，逻辑不聪明”。

---

## 6. write_scope 验证仍然不够硬

你加了 post-run verifier，这是改善。但当前逻辑仍然有漏洞。

`verify_completion_scope` 先读 worker artifact 里报告的：

```text
changed_paths
files_changed
```

然后用：

```bash
git status --porcelain=v1
```

检查当前 worktree。

问题：

第一，`git status` 是整个 repo 当前状态，不是这个 attempt 的状态。多个 worker 并发时，会互相污染。

第二，如果 worker 已经 commit、stash、reset、生成后又清理，`git status` 可能为空；代码里 actual_changed_paths 为空时直接 `Ok(())`。这不能证明没越界写过，只能证明现在干净。

第三，新表 `worktree_manifests` 有 `attempt_id` 字段，但实际 `record_worktree_manifest` 插入时只写了：

```text
task_id
project_root
phase
changed_paths
```

没有 `job_id` / `attempt_id`。所以 attempt-specific diff 还没真正建立。

更聪明的做法是：

```text
attempt_start:
  record git tree hash / dirty baseline / file manifest

attempt_end:
  compute diff against baseline
  bind diff to attempt_id
  verify changed paths against capability scope
```

而不是在完成时看一眼全局 `git status`。

---

## 7. BoardTask 仍然没有降级成纯 projection

你确实不再从 description parse 控制字段，这是大进步。

但现在 `BoardTask.runtime_metadata` 承担了太多 canonical control 责任：

```text
write_scope
must_not_touch
sandbox_profile
capability_grant_ids
task_contract_id
dispatch_metadata
```

`task_runtime_contract()` 还会直接从 BoardTask 里读这些。

这说明 BoardTask 仍然是控制对象，不是 projection。

更聪明的分层应该是：

```text
jobs                 canonical
job_attempts          canonical
capability_grants     canonical
work_leases           canonical
task_contracts        canonical
completion_artifacts  canonical
board_task_views      projection
board_tasks           user-facing view / legacy compatibility
```

现在你新增了 `board_task_views`，但真正控制逻辑还在读 `board_tasks.runtime_metadata`。
这说明迁移只走了一半。

---

## 8. `control_plane_kernel.rs` 名字很硬，但实际上还不是 kernel

这个文件现在只有几百行，而且主要是：

```rust
self.state.shared_memory.handle_action(json!({...}))
```

例如：

```text
record_observation -> shared_memory.handle_action("job_event")
settle_task -> shared_memory.handle_action("worker_settle")
claim_lease -> shared_memory.handle_action("claim")
require_capability -> shared_memory.handle_action("capability_check")
```

这更像 adapter/facade，不像 kernel。

真正的 kernel 应该拥有 typed Rust command：

```rust
CompleteTaskCommand
ClaimLeaseCommand
GrantCapabilityCommand
StartAttemptCommand
CommitArtifactCommand
```

并且在一个事务里完成状态迁移、event append、projection update。
而不是把 JSON action 丢给 shared_memory。

当前结构还是：

```text
control_plane_kernel -> JSON action -> shared_memory mega-handler -> SQL
```

这和“硬内核”的目标还差一层。

---

## 9. Autopilot 改善了，但仍然过大、过聪明、过靠近控制面

好消息：Autopilot 不再直接用 summary 关闭任务了。
坏消息：它仍然有接近 **1 万行**，里面仍然有大量文本 summary/anchor/heuristic 逻辑。

它现在会把 PTY/provider prose 记录为 observation，然后等待 canonical artifact。这个方向对。

但它还保留了一个灰区：对 read-only task，可以从 durable provider final 里 materialize task result artifact。
这对研究/只读任务可能实用，但设计上仍然是：

```text
provider prose -> system-generated completion artifact -> close
```

这不是绝对不能有，但必须被降级成特殊类型：

```text
observed_readonly_completion
```

并且只能用于显式标记允许的任务：

```text
artifact_materialization: autopilot_readonly_ok
```

否则你又把 provider prose 包了一层 artifact，然后重新放回控制面。

---

## 10. checker 有“仪式化检查”的味道

`check-v3-control-plane-kernel-isomorphism.mjs` 目前主要是字符串存在性检查：

```js
requireAll(..., [
  'CREATE TABLE IF NOT EXISTS capability_grants',
  'operation: "spawn"',
  'INSERT INTO work_leases',
  ...
])
```

它能检查“有没有写某些词”，但不能检查：

```text
schema 是否允许 Rust 要插入的 enum value
Rust INSERT 列名是否真实存在
JSONB/text 类型是否匹配
release 是否同步 work_leases
capability_check 是否绑定 subject_id
```

所以它会漏掉上面最严重的几个问题。

这就是 LLM 系统常见的 checker theater：
**看起来有很多 checker，但 checker 验证的是符号一致，不是语义一致。**

真正该加的是数据库级 integration test：

```text
迁移一张空 Postgres
执行 grant_task_capabilities
执行 mission_task_delegate
执行 compute_slot(create)
执行 model_route_outcome_put
执行 worker_settle(done)
验证所有 SQL 能跑通
验证 capability subject mismatch 会被拒绝
验证 release 后 active lease 不存在
```

---

# 所以，新版有没有改善？

有，而且是明显改善。

但它现在处于一个危险阶段：

> 旧版的问题是“太多不可靠 projection 参与控制”；新版的问题是“开始建设 typed control plane，但新增权威没有完全闭合，导致控制面有几处假硬化。”

我会把新版状态描述为：

```text
方向：对
抽象：更好
硬度：不够
一致性：有明显 bug
复杂度：继续上升
```

---

# 27 万行真的有必要吗？

我的判断：

> 如果你把它当“个人本地多 Agent 操作系统 + Board UI + PTY provider adapter + MCP + DB + Lisp SSOT + 自进化 + 记忆系统 + 路由学习 + workflow engine”，27 万 Rust 行不算离谱。
> 但如果按第一性原理，只为了实现 MissionD 的核心价值，27 万 Rust 行过多。

更关键的是，它不只是 27 万 Rust 行。
整个仓库大约是：

```text
Rust       28.1 万行
脚本       9.9 万行
Lisp       7.6 万行
Markdown   4.0 万行
TS/TSX     2.5 万行
OCaml      0.55 万行
总计       53.6 万行左右
```

所以真正的问题不是“27 万行多不多”，而是：

> 这个系统的核心控制闭环，需要这么多权威面吗？

答案是：不需要。

---

## 如果把它当 OS，27 万 Rust 行正常吗？

正常，不算大。

很多真正操作系统、数据库、浏览器、编译器、容器系统、IDE 平台都是百万级甚至千万级代码。
如果 MissionD 的定位真的是：

```text
本地 agent OS
多 provider runtime
权限系统
任务系统
UI
数据库
workflow engine
知识库
PTY/TUI runtime
事件总线
长期记忆
自进化系统
compiler / SSOT
```

那么 27 万 Rust 行不是荒唐数字。

但这里有个关键区别：

**OS 的 kernel 和 OS distribution 不是一个东西。**

一个聪明的 MissionD 应该分成：

```text
missiond-kernel        小、硬、不可绕过
missiond-runtime       worker/provider adapter
missiond-board         UI projection
missiond-memory        可选
missiond-router        可选
missiond-v3-compiler   可选
missiond-evolution     实验性
missiond-research      实验性
```

现在的问题是：
**kernel、runtime、UI、研究系统、知识库、演化系统、checker、历史 work orders 全部堆在一起。**

所以它不像一个小 kernel + plugins，更像一个巨大的 agent monolith。

---

# 第一性原理：MissionD 的主功能到底是什么？

我会把主功能压缩成一句话：

> 给一个目标，安全地分派给一个 worker，在受限 scope/capability 下执行，产生 completion artifact，验证后更新状态和 Board projection。

这句话展开，只需要这些 canonical objects：

```text
Job
JobAttempt
CapabilityGrant
WorkLease
CompletionArtifact
EventLog
BoardTaskView
```

只需要这些核心动作：

```text
create_job
start_attempt
grant_capability
claim_lease
spawn_worker
commit_artifact
verify_artifact
settle_job
project_board_view
```

只需要这些边界：

```text
worker adapter
storage
verifier
scheduler
projection API
```

这就是 kernel。

其他功能都应该问一个问题：

> 没有它，MissionD 还能不能完成“安全分派任务并用 artifact 关闭任务”？

如果答案是能，那它就不是 kernel。

---

# 可以砍砍砍的东西

下面我按“砍了是否影响主功能”分类。

## 必须保留的核心

这些不能砍，或者只能重写得更小：

```text
Postgres canonical tables
Job / Attempt state machine
Completion artifact storage
Capability grant / audit
Lease / claim
Worker spawn adapter
PTY adapter 的最小版本
MCP tool gateway
Board projection 的最小版本
event log
task_delegate
task_result_put
worker_settle
post-run verifier
```

但它们现在也可以瘦很多。
例如 `shared_memory.rs` 不应该是 4000 行 mega-handler；`control_plane_kernel` 应该吃掉其中最核心的一部分。

---

## 可以砍到 feature flag 后面的

这些可以存在，但不应该在 core path 里：

```text
V3 Lisp SSOT / compiler / isomorphism checkers
nightly_evolution
lisp_code_sync
learning_engine
model_route_outcomes / online router learning
codex_replay
skill-store
knowledge plan / plan_dag / workflow / review_gate 大系统
memory extraction
evidence_collector 高级逻辑
agent_execution 高级 workflow
multi-provider profile 大表
semantic-terminal package
复杂 Board Conversations UI
大量 check-* 脚本
work-orders / research / evidence 历史档案
```

这些不是没价值，而是不应该和 kernel 同等地位。

---

## 可以直接考虑删除或外置的

我会最先动这些：

### 1. `.missiond/work-orders`

新版继续增加 work-orders。它们对审计/自进化有用，但对 runtime 没必要。
建议移出发布包，或者放到单独 repo/archive。

### 2. 自进化相关

```text
nightly_evolution
lisp_code_sync
proposal/convergence/report
```

在 kernel 没硬之前，自进化会放大复杂度。
应该先关掉。

### 3. 大量 Lisp runtime shards

如果 Lisp 是设计源头，可以保留。
但 production runtime 不应该依赖复杂 Lisp 周边才能启动。
第一性原理版本可以先只保留 compiled policy JSON，Lisp compiler 变成 dev-only。

### 4. route learning

`model_route_outcomes` 是好想法，但不是 core。
先用静态 router + manual override。等 kernel 稳定后再加在线学习。

### 5. knowledge/workflow 巨系统

`crates/missiond-daemon/src/handlers/knowledge/` 非常大。
仅这个区域就占了 Rust 里很大一块，其中大量 plan、plan_dag、workflow、review_gate、agent_execution 测试和逻辑。

主功能不需要复杂 knowledge workflow。
可以先砍到：

```text
create task
delegate task
attach artifact
settle task
query events
```

### 6. Board 高级 UI

Board 最小功能只需要：

```text
任务列表
任务详情
状态
artifact hash
event timeline
capability/lease 状态
手动 approve/reject
```

复杂 Conversations UI、自动展开、多种交互模式、Jarvis 历史兼容都可以放后面。

### 7. 多 provider 全家桶

先保留一个主 provider adapter，比如 Claude Code 或 Codex。
其他 Gemini、Minimax、Sonnet profile、research router 都可以 plugin 化。

### 8. checkers 大阵列

保留少数高价值 integration tests。
砍掉大量“字符串包含型 isomorphism checker”。

更聪明的测试不是 100 个 checker，而是 10 个真实闭环测试：

```text
empty postgres -> migrations -> delegate -> spawn -> write artifact -> settle -> board view
claim conflict
capability subject mismatch
write_scope violation
done without artifact rejected
model route outcome insert/query
```

---

# 我会怎样瘦身

目标不是把项目砍成玩具，而是拆成两层：

```text
missiond-core-kernel
missiond-full-os
```

## `missiond-core-kernel` 目标

```text
Rust: 25k - 50k 行
SQL: 15 - 25 张核心表以内
Board: 最小 UI
Provider: 1 个 PTY adapter
Scripts: < 5k 行
Lisp: dev-only，不在 runtime 关键路径
```

核心表大概只要：

```text
jobs
job_attempts
capability_grants
capability_audit_events
work_leases
completion_artifacts / task_result_artifacts
event_log
board_task_views
workers / slots
projects
```

甚至第一版可以先不要 `board_tasks` 作为 canonical，只保留 view。

## `missiond-full-os` 可以继续庞大

完整版本可以包含：

```text
memory
workflow
plan DAG
multi-provider router
nightly evolution
V3 Lisp SSOT
Board advanced UI
research system
semantic terminal
skill store
codex replay
```

这样 27 万 Rust 行就不是问题。
问题只在于：现在它们和 kernel 缠在一起。

---

# 最应该立刻修的 8 个点

按优先级：

## P0：修 schema-code 不一致

马上改：

```text
capability_grants.operation 加 spawn
capability_grants.subject_kind 加 system/operator，或改 Rust 不写 system
model_route_outcomes migration 与 Rust INSERT/SELECT 对齐
```

这是硬 bug。

---

## P0：决定 lease 唯一权威

二选一：

```text
只用 shared_claims
或只用 work_leases
```

如果保留 `work_leases`，那：

```text
claim
release
heartbeat
expire
recover
query
```

都必须以同一张表为准，不能 `ON CONFLICT DO NOTHING` 后继续返回成功。

---

## P0：capability enforcement 绑定 caller

`require_capability` 参数应该变成：

```text
task_id
grant_id
subject_kind
subject_id
operation
scope_kind
scope_key
```

所有敏感 tool 都必须传调用主体。

没有 subject binding 的 capability 只是 policy flag。

---

## P1：让 `job_attempts` 真正成为执行单位

现在 `job_attempts` 有表，但没统治执行。

应该变成：

```text
spawn worker => create attempt
pre manifest => attempt_id
artifact => attempt_id
post manifest => attempt_id
settle => attempt_id
```

没有 attempt_id 的 artifact 不允许关闭 write-scoped job。

---

## P1：BoardTask 降级

`task_runtime_contract()` 不要再从 `board_tasks.runtime_metadata` 读 canonical 控制信息。
应该从 `jobs` / `task_contracts` / `capability_grants` / `work_leases` 读。

`BoardTask.runtime_metadata` 可以保留，但只能做 projection/cache。

---

## P1：把 control_plane_kernel 从 facade 变成 owner

不要让 kernel 调：

```text
shared_memory.handle_action(json)
```

应该反过来：

```text
shared_memory tool adapter -> typed kernel command
```

现在的方向还是倒的。

---

## P1：把 post-run verifier 改成 attempt diff

不要依赖全局 `git status`。
要在 attempt start/end 记录 baseline 和 diff。

---

## P2：砍掉“字符串包含型 checker”的权威地位

保留它们作为 lint 可以。
但真正质量门应该是 DB integration tests 和 typed schema tests。

---

# 我的最终判断

新版比上版聪明。
但它体现了一个新的 LLM 式问题：

> LLM 很会把 critique 具象化成新表、新字段、新 checker、新 metadata，但不天然擅长把权威面砍到唯一、把事务边界做到不可绕过、把 schema 和 runtime 强制闭合。

你现在不应该继续加功能。
也不应该继续加 checker。
下一步应该是反向操作：

```text
少一个权威面
少一个 projection 回读路径
少一个 fallback
少一个 JSON action mega-handler
少一个 runtime_metadata 控制来源
少一个表的镜像
少一个 provider prose 参与控制的机会
```

至于 27 万行：

> 作为“完整 agent OS 实验场”，27 万 Rust 行可以接受；作为 MissionD 的核心 kernel，太大。
> 真正聪明的结构应该是 3-5 万行硬 kernel + 20 多万行插件/实验/OS distribution，而不是 27 万行 monolith。

如果你只想保留主要功能，我会把目标定成：

```text
保留：安全派工、capability、lease、artifact、attempt、event、Board projection、一个 worker adapter。
砍掉/外置：自进化、Lisp 编译链 runtime 依赖、复杂 knowledge workflow、route learning、高级 Board conversations、多 provider 全家桶、大量字符串 checker、work-orders 历史包。
```

最关键的一句话：

> 新版从“文本控制系统”进化成了“typed control-plane 雏形”，这是对的；但真正聪明的下一步不是继续长大，而是把 kernel 从这个庞然大物里剥离出来，让所有其他东西都变成插件、projection 或实验层。
