# 2026 年的 AI 视频模型,能仅凭"背影抖动"数清机枪射出了多少发子弹吗?——一份能力评估

## 一句话结论

- **不能。** 截至 2026 年 6 月,没有任何一个通用多模态大模型——包括你说的"Gemini 3.5 Flash"(谷歌 5 月 19 日正式上线、目前真实存在的旗舰 Flash 模型)——能够仅靠画面里身体的后坐抖动,可靠地一发一发数清机枪的射击次数。卡死它的不是聪明程度,而是一道硬邦邦的物理/工程天花板:机枪每秒打出约 12–17 发,而这些模型默认每秒只抽 1 帧画面,即便手动调到上限也只有 24 fps——刚好顶到、甚至低于"采到这种高频事件"所需的奈奎斯特采样率。
- **真正该用的工具不是聊天大模型,而是专门做"时序重复动作计数(RAC)"的计算机视觉模型**(比如 RepNet、TransRAC、ESCounts、CountLLM),而且必须喂给它高帧率素材。但就连这些专家模型也只是为俯卧撑这类每秒 1–5 次的缓慢周期动作设计的,它们能分辨的最快频率同样被奈奎斯特极限锁死(RepNet 能解析的最短周期是 2 帧),没有一个在"每秒 10 发以上"的射击场景上验证过。
- **想在今天就把这个数算个八九不离十,有可行的工程方案,但都带前提:** 要么真的拿到高速摄影素材(240 fps 起步),用检测器去数枪口焰或抛壳;要么——更靠谱——干脆别去数单发,而是用"射击持续时长 × 武器已知的理论射速"算出来。至于纯靠"背影抖动"去数,是所有信号里最不可靠的一种,2026 年没有任何模型能把它做准。

## 几个关键发现

**1. 先纠个名:你说的"Gemini 3.5 Flash"其实没说错。** 这次不用担心叫错型号——谷歌确实有一个几乎一模一样名字的模型。截至 2026 年 6 月 1 日,**Gemini 3.5 Flash 是真实存在的**,是谷歌最新的、已正式商用(GA)的 Flash 档模型,在 5 月 19 日的 Google I/O 上发布后即开放。它所在的产品线里还有 **Gemini 3 Pro**、**Gemini 3.1 Pro**(2026 年 2 月发布的纯推理旗舰)、**Gemini 3 Flash**、**Gemini 3.1 Flash-Lite**,以及一个叫 **Gemini Omni** 的视频生成模型。所以"一个像 Gemini 3.5 Flash 这么强的模型"是个站得住脚的当下参照系——它是带原生视频理解能力的前沿模型。你的叫法基本是对的。

**2. 同档竞品长什么样(2026 年 6 月)。** 三家前沿实验室都出了原生多模态、能直接吃视频的模型:谷歌的 **Gemini 3.5 Flash / 3.1 Pro**;OpenAI 的 **GPT-5.5**(2026 年 4 月 23 日发布,号称"原生全模态",在同一套架构里统一处理文字、图像、音频和视频);Anthropic 的 **Claude Opus 4.7**(2026 年 4 月 16 日)和 **Opus 4.8**。开源阵营里,阿里的 **Qwen3-VL** 系列在视频上最值得关注,它用显式的"文本-时间戳对齐"做时序定位(取代了原来的 T-RoPE)。但在"数子弹"这件事上,它们撞的是同一堵墙(见下)。

**3. 帧率才是真正的决定性瓶颈。** Gemini 的视频处理管线**默认每秒只抽 1 帧(1 FPS)**,这一点在谷歌官方 Gemini API 文档里写得清清楚楚("默认从视频中按每秒 1 帧采样")。谷歌自己的文档甚至原话警告:**"请注意,快速动作序列可能会因 1 FPS 的采样率而丢失细节。"** 你可以通过自定义 `fps` 参数把采样率调高,但**上限是 24 FPS**(文档给出的取值区间是 `(0.0, 24.0]`,出自谷歌 Vertex AI REST API 参考文档,Google Cloud 社区作者 Laurent Picard 也佐证了这一点)。Gemini 3 Pro 在谷歌自己的演示里"优化"到最高 **10 FPS**(即默认值的 10 倍),并不是 60。而实时的 Live API 被硬锁在 1 FPS——谷歌明确表示这让它**"不适合需要分析快速变化画面的场景,比如高速体育的逐回合解说"**。

**4. 对全自动连发来说,这笔账根本算不过来。** 以 M249 班用机枪为例,它的理论射速大约是**每分钟 700–850 发**——美军规范 MIL-DTL-70446C 第 3.13 条原文:*"每挺 M249 机枪配主枪管及指定枪管时,理论射速应在每分钟 700 至 850 发之间"*(约每秒 12–14 发;早期的双气体调节器还允许冲到 1,000 rpm,即每秒约 17 发)。而梗里那挺米尼岗(转管机枪)的射速被夸张成虚构的 10,000 rpm,约每秒 166 发。要想**把离散事件一个一个数出来**,采样率至少得是事件频率的 2 倍(奈奎斯特定理)。在 Gemini 的绝对上限 24 FPS 下,你最多只能分辨出每秒约 12 个不同事件——这**低于**真实机枪的射速,更是远远够不到米尼岗。默认 1 FPS 下,这事彻底没戏。更糟的是:就算开到 24 FPS,一段普通的 24–30 fps 源视频里**根本就不包含**那些信息——想把后坐细节拍清楚,本就需要高速相机(现代手机能到 240 fps,真正的弹道后坐分析要 1,000 fps 以上)。

**5. 抛开帧率不谈,多模态大模型在"视频里数数"这件事上本身就很差。** 这在各类基准测试里有据可查。在 **TemporalBench** 上,模型"在动作频率(计数)这一项上栽得最惨"。在 2026 年的 **EC-Bench**(长视频计数)里,论文写道:*"在 22 个被测的多模态大模型(MLLM)中,最好的模型在'枚举'任务上只有 29.98% 的准确率、'计数'任务上 23.74%,而人类分别能到 78.57% 和 82.97%。"* 所以它的短板是双重的:既**看不见**快速事件,就算事件清清楚楚摆在那儿,它也**数不准**。

**6. 专门的 RAC 模型才是这一领域真正的最高水平——但也只擅长慢动作。** 这个专门的计算机视觉任务叫"重复动作计数(Repetitive Action Counting,RAC)",对应的基准是 **RepCount、UCFRep、Countix**。主流方法有:**RepNet**(谷歌,类别无关)、**TransRAC**(Transformer 架构,CVPR 2022)、**ESCounts**(基于样例)和 **CountLLM**(CVPR 2025,基于大模型,当前 SOTA)。按 CountLLM 论文的说法,在 RepCount 数据集上配 VideoMAE 编码器,它*"达到 0.639 的 OBO 准确率和 0.162 的 MAE,显著超过 TransRAC(0.410 OBO)和 ESCounts(0.563 OBO)"*。这些都是为健身计数这类**每秒 1–5 次(1–5 Hz)**的动作设计的——而且 RepNet 的架构预测周期长度最短只能到 **2 帧**(奈奎斯特下限)。没有任何一个是为"每秒 12–166 次"这种量级构建或验证过的。

## 详细展开

### "Gemini 3.5 Flash"到底是什么,它拿视频能干什么

谷歌于 2026 年 5 月 19 日发布 **Gemini 3.5 Flash**(稳定模型串 `gemini-3.5-flash`),定位是"以 Flash 的速度提供前沿智能",宣称"在多模态理解上领先(CharXiv Reasoning 得分 84.2%)",原生支持文字、音频、图像、代码和视频,上下文窗口 100 万 token(输出 6.4 万 token)。它能在默认媒体分辨率下吃下约一小时的视频(100 万上下文),低分辨率下可达三小时。

对于你描述的那种"计数视频"——真实素材,后期叠加一个成本计数器——Gemini 完全能做到:(a) 认出画面里是个士兵在打机枪;(b) 读出并转写屏幕上的任何文字;(c) 只要给它理论射速,它就能把成本算清楚。**它做不到的,是独立、精确地从射手身体那点细微后坐里,一发一发把子弹数出来。**

### 为什么"靠后坐抖动"恰恰是最难走的一条路

三个相互独立的问题叠在一起:

1. **时间分辨率不够。** 哪怕开到 24 FPS 上限,面对每秒 12–17 发的武器,你也只是顶到或低于奈奎斯特率。一次次后坐冲击会糊成一片连续的颤动,模型根本拿不到"每发一帧"的画面。
2. **信号太弱。** "背影抖动"是个二阶的、低幅度的线索——比枪口焰、比抛壳要弱得多。RAC 领域的研究反复指出,镜头运动、运动模糊、视角变化都会拉低计数精度;而一段手持的实战片段,正好是最糟糕的情况。
3. **数数本身就弱。** 正如 EC-Bench 和 TemporalBench 所揭示的,即便每个事件单独看都清清楚楚,当下的多模态大模型也很难把重复事件正确地累加成一个数字。

### 专门的 CV 模型能做什么、不能做什么

RAC 文献是正确的参照框架。**RepNet** 从逐帧的特征向量里构建一个"时序自相似矩阵",预测逐帧的周期性;因为它是类别无关的,所以原则上它可以尝试去数任何周期性运动,包括后坐。但它的周期长度预测器,对一个 64 帧的输入窗口,只输出 {2, …, 32} 帧这个离散范围里的类别——也就是说,**它能检测的最短周期是 2 帧(奈奎斯特极限)**,而且只能往**更长**的方向扩(通过 5 倍变速回放可延伸到 160 帧),**不能往更短走**。

所以,假设输入是 30 fps,RepNet 能数清的最快节奏约为每秒 15 次——而这还只是理论天花板,并非在枪声上验证过的可用工作点。要数清每秒 166 发的米尼岗,你需要源素材远超约 332 fps,并且模型要为这个量级重新训练;现成的东西没一个能做到。况且这些模型调校所用的健身数据集(RepCount、UCFRep、Countix),平均每次重复都长达好几秒。

### 真正能跑通的务实方案

如果有人真想自动生成这种"每发多少钱"的视频:

- **首选(算术法)。** 检测每段连射的**起点和终点**(这对任何模型都是小菜一碟,哪怕 1 fps,有音频就更简单),再用连射时长乘以武器公布的理论射速(比如 M249 ≈ 800 rpm)。这样不用数单发就能得到准确的弹数——而这恰恰就是当年那个"12 秒花掉 40 万美元"的《军团要塞 2》米尼岗梗的原始算法(10,000 rpm × 0.2 分钟 × 每发 200 美元)。
- **次选(枪口焰/抛壳检测)。** 在真正的高速素材(240 fps 以上)上训一个目标检测器(YOLO 类),逐帧检测离散的枪口焰或弹出的弹壳,再累加起来。枪口焰检测是成熟技术,已经用在实战部署的枪声检测系统里。
- **最差(后坐抖动)。** 也就是你专门问的那个——纯靠身体抖动去数——是所有信号里最不可靠的一种,2026 年没有任何模型能做好。

## 建议

1. **别用聊天大模型(Gemini 3.5 Flash、GPT-5.5、Claude Opus 4.7)去数子弹。** 用它做它真正擅长的部分:识别武器、转写屏幕文字、配解说、算成本账。**复评触发条件:** 如果未来某个模型有据可查地证明了"有效采样率 >60 fps 下的可靠计数",并在某个快速事件基准上达到高 off-by-one 准确率,那时再重新评估。
2. **数子弹这件事本身,用"时长 × 理论射速"法。** 它稳健、可解释,而且正好契合那个梗自己的内在逻辑。让大模型去检测连射的起止时间戳(它在低帧率下也能做到),再把武器的 rpm 喂给它。
3. **如果你非要做逐发的视觉计数,就在高速源视频上搭一条两阶段 CV 管线:**(a) 确保素材 ≥240 fps;(b) 跑一个微调过的枪口焰/抛壳检测器(或一个为高节奏重训过的 RAC 模型);(c) 去重并累加。把后坐抖动只当成兜底信号。
4. **把精度预期摆正。** 即便是专门的 RAC 模型,在**慢动作**上、在困难数据集上,"off-by-one 正确"也只有约 56–64%(RepCount 上 ESCounts 0.563、CountLLM 0.639 OBO)。在 2026 年,纯靠画面做"帧级精确的数枪",应当被视为研究级别的课题,而非可直接上生产的能力。

## 几点说明与免责

- **厂商文档和 FPS 上限会变。** 谷歌明确指出,采样率"未来可能为提升推理效果而调整"。24 FPS 上限是截至 2026 年 6 月的文档值;就算谷歌把它提上去,奈奎斯特那笔账会改善,但这本身既修不好计数能力的短板,也消除不了对高速源素材的依赖。
- **信源质量参差不齐。** 部分关于模型能力的说法来自厂商博客和聚合站点;而那些定量的基准数字(EC-Bench、RepCount/CountLLM)出自同行评审/arXiv 论文,更可靠。这个区别我在全文里都做了标注。
- **米尼岗那个梗用的是虚构数字。** "40 万美元 / 10,000 rpm / 12 秒"这套说法出自 Valve 的《军团要塞 2》"Meet the Heavy"宣传片,不是真实武器的参数;真实机枪的理论射速(700–1,000 rpm)要低得多,就连实战部署的米尼岗也就 2,000–6,000 rpm 封顶。
- **"像 Gemini 3.5 Flash 这么强"是个移动靶。** Gemini 3.5 Pro 据称已在测试中、"下个月(约 2026 年 6 月)推出";但一个更强的通用模型,并不会改变这个特定的高节奏计数任务在采样/奈奎斯特上的根本限制。出路是一条专门的管线,而不是一个更大的聊天机器人。
